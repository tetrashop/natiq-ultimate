# نیازمندی‌های اصلی برای Termux
# نصب با: pip install -r requirements_termux.txt

# پایه‌های علمی
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# پردازش زبان طبیعی
transformers>=4.30.0
sentencepiece>=0.1.99
tokenizers>=0.13.0
protobuf>=3.20.0
accelerate>=0.20.0

# ابزارهای کمکی
requests>=2.28.0
tqdm>=4.64.0
colorama>=0.4.6  # برای رنگ در ترمینال

# لاگ‌گیری و نظارت
psutil>=5.9.0

# نکته: PyTorch در Termux نیاز به نصب جداگانه دارد
# دستور نصب PyTorch:
# pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cpu
