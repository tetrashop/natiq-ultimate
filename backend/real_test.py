#!/usr/bin/env python3
"""
تست عملیاتی واقعی - حتی بدون PyTorch
"""

import sys
import time

class SimpleChatBot:
    """یک چت بات ساده برای تست"""
    
    def __init__(self):
        self.responses = {
            "سلام": ["سلام! چطور می‌تونم کمک کنم؟", "سلام عزیزم! حالتون چطوره؟"],
            "حال": ["من خوبم ممنون! شما چطورید؟", "خوبم، مرسی که پرسیدید!"],
            "اسم": ["من natiq-ultimate هستم، دستیار هوشمند فارسی شما!", "اسم من natiq هست!"],
            "هوش مصنوعی": [
                "هوش مصنوعی شاخه‌ای از علوم کامپیوتر است که به ساخت ماشین‌های هوشمند می‌پردازد.",
                "AI یا هوش مصنوعی توانایی یک ماشین برای تقلید رفتار هوشمندانه انسانی است."
            ],
            "چطوری": ["خوبم! چطور می‌تونم کمک کنم؟", "عالیم! چیزی لازم داری؟"],
            "خداحافظ": ["خداحافظ! موفق باشید.", "بای بای! بازم سر بزنید."],
        }
        
        self.default_responses = [
            "جالب بود! در موردش بیشتر بگو.",
            "منظورت رو کامل متوجه نشدم، می‌تونی دقیق‌تر بگی؟",
            "فعلاً جواب این رو نمی‌دونم، اما در حال یادگیری هستم!",
            "سوال خوبیه، بذار یه فکر کنم...",
        ]
    
    def respond(self, question):
        """پاسخ به سوال"""
        question_lower = question.lower()
        
        # جستجوی کلیدواژه
        for key in self.responses:
            if key in question_lower:
                import random
                return random.choice(self.responses[key])
        
        # پاسخ پیش‌فرض
        import random
        return random.choice(self.default_responses)

def main():
    print("🤖 natiq-ultimate - نسخه تست ساده")
    print("=" * 50)
    print("این یک نسخه آزمایشی است که حتی بدون PyTorch هم کار می‌کند!")
    print("برای خروج 'خروج' بنویسید.")
    print("-" * 50)
    
    bot = SimpleChatBot()
    
    while True:
        try:
            question = input("\n📝 شما: ").strip()
            
            if question.lower() in ['خروج', 'exit', 'quit']:
                print("👋 خداحافظ!")
                break
            
            if not question:
                print("⚠️ لطفاً یک سوال وارد کنید")
                continue
            
            print("🤔 در حال پردازش...")
            time.sleep(0.5)  # شبیه‌سازی پردازش
            
            answer = bot.respond(question)
            print(f"🤖 ربات: {answer}")
            
        except KeyboardInterrupt:
            print("\n👋 خداحافظ!")
            break
        except Exception as e:
            print(f"❌ خطا: {e}")

if __name__ == "__main__":
    main()
